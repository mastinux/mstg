

//var class_name = Java.use("package.path.to.className");
//
//class_name.overloadedMethod
//	.overload('first.argument.package.path.to.className', 'second.argument.package.path.to.className')
//	.implementation = function (first_arg, second_arg) {
//		console.log(first_arg)
//
//		return this.overloadedMethod(first_arg, second_arg)
//	}

Java.perform(function () {
	try {
		// Our code snippets go here.
	}
	catch(e) {
		console.log(e.message);
	}
});

// sg.vp.owasp_mobile.omtg_android